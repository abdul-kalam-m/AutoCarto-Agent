name: autocarto-agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11.8
  - pip=24.0
  - numpy=1.26.4
  - scipy=1.12.0
  - pandas=2.2.1
  - geopandas=0.14.4
  - shapely=2.0.3
  - pyproj=3.6.1
  - pyogrio=0.7.2
  - matplotlib=3.8.4
  - contextily=1.6.2
  - cartopy=0.22.0
  - rasterio=1.3.10
  - xarray=2024.2.0
  - dask=2024.2.1
  - psycopg2=2.9.9
  - libpysal=4.9.2
  - esda=2.5.1
  - jenkspy=0.3.2
  - pip:
      - qdrant-client==1.8.1
      - openai==1.14.3
      - colour-science==0.4.6
      - colorspacious==1.1.2
      # PATCH: the original "visvalingam-whyatt==0.2.1" does not exist on PyPI;
      # the actual package is "visvalingamwyatt" (no hyphen) and 0.2.0 is the
      # current release. Pinned to keep the lock reproducible.
      - visvalingamwyatt==0.2.0
